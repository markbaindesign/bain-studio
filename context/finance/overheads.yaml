---
name: studio_overheads
description: Monthly fixed costs for margin and viability calculations. Itemised from GnuCash — authoritative live figures always in context/finance/accounts.json.
---

# Monthly Fixed Costs (Overheads)

# Itemised from GnuCash upcoming_all fixed entries.
# Update this file when subscriptions change.
# The runtime total (fixed_costs) comes from accounts.json — trust that over this file.

monthly_fixed:
  autonomo_social_security: 379.97   # Spanish autónomo mandatory contribution (note: overheads.yaml previously had 300 — GnuCash shows 379.97; confirm with Mark)
  gestor_accountant: 212.00           # Monthly accountant / gestora fee
  movistar_phone_internet: 102.81     # Phone and internet (Movistar)
  crashplan_backup: 44.08             # CrashPlan backup service
  asana: 26.98                        # Asana project management
  claude_anthropic: 18.00             # Claude API credits (Hermes + Athena + Plutus)
  cloudways_hosting: 10.10            # Cloudways hosting (studio server)
  algolia: 4.04                       # Algolia search (client project)

# Derived totals — read from accounts.json at runtime for live figures
reference_totals:
  fixed_costs_approx: 717.38          # From GnuCash (accounts.json: fixed_costs)
  owner_draw_monthly: 2566.67         # Mark's monthly draw (accounts.json: owner_draw)
  breakeven_allin: 4115.00            # Fixed costs + owner draw + buffer (accounts.json: breakeven_allin)

# Rate and margin thresholds are in rates.yaml
